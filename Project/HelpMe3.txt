📦 مكتبات بايثون القياسية (Built-in)
importlib → لتحميل الموديولات ديناميكيًا أثناء التشغيل.

inspect → لفحص الكائنات واستخراج معلومات عن الدوال، الكلاسات، التوقيعات، والكود المصدري.

io → لإنشاء قنوات إدخال/إخراج في الذاكرة (مثل StringIO) لالتقاط النصوص.

sys → للتحكم في مخرجات البرنامج وإعادة توجيه stdout.

pydoc → (مستوردة لكن لم تُستخدم في الكود الحالي) لعرض التوثيق تلقائيًا.

textwrap.wrap → لتقسيم النصوص الطويلة لأسطر قصيرة منسقة.

typing → لتعريف أنواع البيانات (Type Hinting) مثل Any، List، Dict، Tuple، Optional.

keyword → لمعرفة الكلمات المحجوزة في بايثون.

collections.defaultdict → لإنشاء قواميس بقيم افتراضية تلقائيًا.
============================================================================================
[المستخدم]
    │
    ▼
[LibraryExplorer Class]
    │
    ├── إدارة التاريخ و المسار الحالي و الإشارات المرجعية
    │
    ├── [load_object()]
    │       ├── importlib → استيراد الموديول
    │       ├── getattr → الحصول على عناصر الموديول
    │       └── __builtins__ → دعم الدوال المدمجة
    │
    ├── [display_members()]
    │       ├── inspect.getdoc → وصف الكائن
    │       ├── classify_members()
    │       │       ├── inspect.isclass / callable → تصنيف العناصر
    │       │       ├── keyword.iskeyword → تجاهل الكلمات المحجوزة
    │       │       └── repr → عرض القيم
    │
    ├── [show_help()]
    │       ├── inspect.signature → عرض توقيع الدالة
    │       ├── get_source_code() → inspect.getsource
    │       └── get_help_text() → help() مع io.StringIO و sys.stdout
    │
    ├── [search_members()]
    │       └── مقارنة النصوص باستخدام query
    │
    ├── [show_source()]
    │       └── inspect.getsource → عرض الكود المصدري
    │
    ├── [show_builtins()]
    │       └── __builtins__ + dir → عرض الدوال المدمجة
    │
    ├── [install_module()]
    │       └── عرض أوامر pip للتثبيت
    │
    └── [interactive_loop()]
            └── تنفيذ الأوامر من المستخدم وتوجيهها لباقي الدوال
=============================================================================================
3. شرح طريقة التفكير (منطق العمل)
التهيئة (Initialization)

إنشاء متغيرات للتاريخ (history)، المسار الحالي (current_path)، الكائن الحالي (current_object)، والإشارات المرجعية (bookmarks).

تجهيز ألوان ANSI للعرض المنسق في الطرفية.

التفاعل مع المستخدم (Interactive Loop)

البرنامج ينتظر أوامر من المستخدم مثل:

اسم موديول (os.path)

اسم دالة مدمجة (len)

أوامر خاصة (help, search, back, history, exit...)

يفكك الأمر لمعرفة الكلمة الأساسية (command) والمعامل (arg).

تحميل واستكشاف الموديولات أو الكائنات

إذا أدخل المستخدم اسم موديول، يتم استيراده باستخدام importlib.

إذا كان Built-in، يُستخدم __builtins__.

يتم عرض أعضائه مصنفة إلى:

دوال

كلاسات

خصائص

الفحص (Inspection)

عرض التوقيع (inspect.signature) إذا كان Callable.

عرض الكود المصدري (inspect.getsource) إذا كان متاحًا.

عرض التوثيق (help() مع التقاط النص عبر io.StringIO).

البحث (Search)

البحث في أسماء ووصف الأعضاء لمطابقة الكلمة المفتاحية.

التنقل (Navigation)

الرجوع للخلف (go_back)

عرض التاريخ (show_history)

حفظ واسترجاع الإشارات المرجعية (bookmark_location, goto_bookmark)

الإضافات

عرض جميع الـ Built-ins (show_builtins).

إعطاء تعليمات تثبيت أي موديول مفقود (install_module).

============================================================================================
